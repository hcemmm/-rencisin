<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bitirme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bitirme">
        <activity android:name=".UpdateProfilActivity" />
        <activity android:name=".AnaProfilActivity" />
        <activity android:name=".YurtActivity" />
        <activity android:name=".ParkActivity" />
        <activity android:name=".RestorantActivity" />
        <activity android:name=".KirtasiyeActivity" />
        <activity android:name=".CafeActivity" />
        <activity android:name=".AvmActivity" />
        <activity android:name=".UniversiteActivity" />
        <activity android:name=".HakkimizdaActivity" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".AnasayfaActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>